// INITILIZE CONTROLLER
// ============================================================
angular.module("brb").controller("beers", function($scope) {
  // VARIABLES
  // ============================================================
  $scope.test = "beers test"
  // FUNCTIONS
  // ============================================================
});
